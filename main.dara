init()

model ICredentials = {
  accessKeyId?: string,
  accessKeySecret?: string,
  securityToken?: string,
  providerName?: string,
}

async function getCredentials(): ICredentials

function getProviderName(): string